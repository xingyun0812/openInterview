收到，按 No-Go 执行。现在进入契约统一阶段，禁止并发合并。

请在 完成以下任务并回报：
1) 对 RFC-1~RFC-4 给出最终推荐方案（每个至少2个备选+优缺点+最终建议）
2) 我确认后，产出“统一契约基线 v1”：
   - 字段字典（含 screen_status、assistant字段）
   - 枚举字典（含 export_type）
   - 事件字典（MQ/Webhook命名映射）
   - 接口字典（request/response关键字段）
3) 给出迁移与兼容策略（是否双写/兼容旧字段）
4) 更新受影响的测试清单与回归范围
5) 明确“可解冻条件”

未完成以上内容前，所有相关分支禁止合并。